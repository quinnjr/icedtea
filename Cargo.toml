[workspace]
resolver = "2"
members = ["contract", "config", "compositor", "clipboard", "harness", "shell", "settings", "notifications", "ui"]

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.94"

[workspace.dependencies]
zbus = "5"
redb = "3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
crossbeam-channel = "0.5"
image = "0.25"

# TEMPORARY, dropped in P2's last commit: P2–P8 develop against P1's wlr
# worktree before 0.20.28 is published. Exactly the shape the implicit-grab
# fix used (develop @ 90813cc reverted it the same way). `cargo publish` and
# the merge both require this gone.
[patch.crates-io]
wlr = { path = "/home/joseph/Projects/wlroots-sys/.claude/worktrees/wlr-m3-popups/crates/wlr" }
