[package]
name = "icedtea-shell"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
icedtea-config = { path = "../config" }
icedtea-contract = { path = "../contract" }
icedtea-ui = { path = "../ui" }
wayland-protocols-wlr.workspace = true
xkbcommon.workspace = true
zbus.workspace = true
async-channel = "2"
futures-util = "0.3"
tracing.workspace = true
tracing-subscriber.workspace = true

[dev-dependencies]
icedtea-harness = { path = "../harness" }
icedtea-clipboard = { path = "../clipboard" }
# The page table and app id in `launcher::provider` mirror this crate; the
# `settings_pages_and_app_id_stay_in_sync_with_icedtea_settings` test is the
# gate (the shell crate's production build does not depend on settings).
icedtea-settings = { path = "../settings" }
crossbeam-channel.workspace = true
